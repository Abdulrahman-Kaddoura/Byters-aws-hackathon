# Runtime: boto3 is preinstalled in the AWS Lambda Python runtime, but pinning
# it makes local development and tests reproducible.
boto3>=1.34.0
pypdf>=4.0.0
python-docx>=1.1.0

# --- Dev / test only (not needed at runtime) ---
pytest>=8.0.0
moto[dynamodb,cognitoidp]>=5.0.0
